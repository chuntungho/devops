version: '3.6'

volumes:
  redis-data:

services:
  redis:
    image: redis:5.0.9-alpine
    volumes:
      - redis-data:/data
    ports:
      - 6379:6379
    command: ["redis-server", "--requirepass redisPwd"]