version: '3.8'
services:
  mysql:
    image: mysql
    container_name: mysql8
    volumes:
      - ./data:/var/lib/mysql/
      - ./my.cnf:/etc/mysql/conf.d/my.cnf      
    environment:
      - "MYSQL_ALLOW_EMPTY_PASSWORD=true"
      - "TZ=Asia/Shanghai"
    ports:
      - 3306:3306